# Happy Vision Pre-Dogfood Hardening Checklist

**目標：** 同事首次實測前，系統性覆蓋常見 edge case，把潛在 bug 在上線前挖出來修掉。

**規則：**
- `[ ]` 未處理 ｜ `[x]` 已覆蓋（加上 test 檔案路徑）｜ `[SKIP-需人工]` 無法自動驗證
- 一輪只處理一個項目
- 新測試放在 `tests/hardening/`
- Gemini API 一律 mock，不打真實 request
- 每個修復 commit 一次，訊息含 edge case 描述

---

## A. 檔案系統與路徑（pipeline.py / folder_watcher.py / metadata_writer.py）

- [x] A1. 路徑含空格（`/tmp/my photos/test.jpg`）能正確分析與寫入 metadata — `tests/hardening/test_a1_path_with_spaces.py`（4 tests，已覆蓋，無 bug）
- [x] A2. 路徑含中文（`/tmp/測試照片/阿公生日.jpg`）能正確處理 — `tests/hardening/test_a2_path_with_cjk.py`（3 tests，含 resume key 驗證）
- [x] A3. 路徑含 emoji 或特殊符號能正確處理 — `tests/hardening/test_a3_path_with_emoji.py`（5 tests：emoji 檔名 / emoji 父資料夾 / SQLite round-trip / exiftool args / NFC-NFD 正規化）
- [x] A4. 副檔名大小寫混合（`.JPG` / `.jpg` / `.JPEG` / `.jpeg`）都能被識別 — `tests/hardening/test_a4_extension_case_mixing.py`（4 tests + scanner-agreement 回歸）
- [x] A5. macOS HFS+ / APFS case-insensitive 檔名衝突不會重複處理 — `tests/hardening/test_a5_case_insensitive_fs.py`（4 tests，含 xfail 鎖住已知限制：不同 case folder path 被當成不同 key 會重做分析。Mitigation：檔案選擇器回 canonical casing）
- [x] A6. symlink 指向的 JPG 能處理，指向不存在檔案時優雅失敗 — `tests/hardening/test_a6_symlinks.py`（6 tests：symlink to file / folder、broken symlink skip、pipeline 不 crash、analyze_photo broken return None、symlink loop < 2s）
- [x] A7. 巢狀資料夾（多層子目錄）遞迴掃描正確 — `tests/hardening/test_a7_nested_folders.py`（5 tests：5 層巢狀全抓 / 混合 JPG+非 JPG+sidecar / 隱藏目錄不當 photo / 空樹回 [] / pipeline 全程跟）
- [x] A8. 空資料夾 / 只含非 JPG 檔案的資料夾 → 清楚錯誤訊息，不 crash — `tests/hardening/test_a8_empty_or_no_jpg_folder.py`（7 tests：空/非 JPG/不存在/檔案路徑/pipeline 不呼叫 API/report 空匯出）
- [x] A9. 超長路徑（> 255 chars）處理 — `tests/hardening/test_a9_long_paths.py`（4 tests：240-char name / 300+ char total path / SQLite 400+ char round-trip / 超過 NAME_MAX 優雅 skip）
- [x] A10. 唯讀資料夾 / 檔案 → metadata 寫入失敗時不污染 result_store — `tests/hardening/test_a10_readonly_folder.py`（5 tests，**發現 macOS exiftool 可突破檔案 0o444，必須用 dir 0o555 才能真 block**；session 在失敗後仍能繼續寫別的檔）

## B. 照片檔案本身（gemini_vision.py / metadata_writer.py / phash.py）

- [x] B1. 損壞的 JPG（檔頭錯、截斷、0 bytes）→ skip + 記錄於 result_store，不 crash — `tests/hardening/test_b1_corrupted_jpg.py`（5 tests，**修掉真實 bug**：`UnidentifiedImageError` 原本會炸掉整個批次）
- [x] B2. 偽裝副檔名（`.jpg` 實際是 PNG / HEIC / TIFF）→ 清楚錯誤，不誤判 — `tests/hardening/test_b2_fake_extension.py`（5 tests，**修掉 mime/payload mismatch bug**：`resize_for_api` 之前在小檔時 return 原 bytes，PNG 會以 `image/jpeg` 流到 Gemini）
- [x] B3. 超大尺寸（> 50MB 或 > 8000x8000）記憶體安全 — `tests/hardening/test_b3_oversized_images.py`（4 tests，含 DecompressionBomb 模擬：靠 B1 的 catch 安全失敗）
- [x] B4. 照片無 EXIF → 不 crash，metadata 照寫 — `tests/hardening/test_b4_photo_without_exif.py`（4 tests，含 byte-level APP1 segment absence check）
- [x] B5. 照片已有 IPTC/XMP metadata 衝突 → 依設定覆蓋或保留 — `tests/hardening/test_b5_existing_metadata_conflict.py`（6 tests，**修掉真實 bug**：`-IPTC:Keywords=` 對 list-type 是先清後設，使用者手動打的「wedding, bride」會被 AI 的 keywords 洗掉。改用 `+=` 合併；scalar tag 仍用 `=` 覆蓋）
- [x] B6. 同一張照片重複執行 → phash 去重有效，不重複 call Gemini — `tests/hardening/test_b6_phash_dedup.py`（6 tests：pipeline `skip_existing=True` 跳過已處理 / 失敗照片重試 / 預設 `skip_existing=False` lock-in / 已知限制：pipeline 不做跨 path phash 比對 / find_similar byte-identical copy 命中 distance=0 / find_similar 忽略 failed rows）。folder_watcher phash dedup 已有 `tests/test_folder_watcher_dedup.py` 4 tests 覆蓋，不重複。
- [x] B7. 黑白 / 去色 / 低解析度 / 純色照片 → Gemini 分析失敗時的 fallback — `tests/hardening/test_b7_low_info_photos.py`（8 tests：純黑/1x1/灰階 resize 不 crash / minimal response 填 defaults / 空 keywords 不出空 arg / pipeline 當 completed 存 / solid color smoke）

## C. Gemini API 與網路（gemini_vision.py / gemini_batch.py / rate_limiter.py）

- [x] C1. 無網路 / DNS 失敗 → 清楚錯誤訊息 + 保留部分結果 + 可 resume — `tests/hardening/test_c1_no_network.py`（5 tests，**修掉真實 bug**：DNS / connect / timeout 錯誤之前被當成不可重試，一次就 fail；現在納入 retryable markers，可重試，batch 不 halt，offline 後 resume 可跑完）
- [x] C2. Gemini API timeout（模擬 30s+ 無回應）→ 重試機制啟動，最終放棄時優雅失敗 — `tests/hardening/test_c2_api_timeout.py`（5 tests：DEADLINE_EXCEEDED / socket.timeout / ReadTimeout 皆可重試 + thread 不會永遠阻塞）
- [x] C3. 401 invalid API key → 立即停止，不繼續燒 quota，提示使用者去設定 — `tests/hardening/test_c3_invalid_api_key.py`（5 tests，**新增 `InvalidAPIKeyError` + pipeline halt**）
- [x] C4. 429 rate limit → rate_limiter 正確退避，不 crash — `tests/hardening/test_c4_rate_limit_429.py`（6 tests：retry 成功/失敗/batch 續跑 + rate_limiter refill/close 單元測試；用 mock time.sleep 讓 tests <1s）
- [x] C5. 403 quota exceeded → 停止批次，標記剩餘為 pending 可 resume — `tests/hardening/test_c5_quota_exceeded.py`（3 tests，鎖死 halt-then-resume 合約：剩餘照片保持無 row 狀態，避免 resume 死循環）
- [x] C6. 500 / 503 server error → 重試，最終放棄時保留進度 — `tests/hardening/test_c6_server_errors.py`（6 tests：500/502/503/504 分別重試 + batch 不因單張 500 halt + 500 不匹配 auth fatal markers）
- [x] C7. 回傳 JSON 格式錯誤 / 截斷 → parse 失敗時該張標記錯誤，不中斷整批 — `tests/hardening/test_c7_json_parse_errors.py`（12 tests，**修掉 TypeError bug**：`parse_response("[]")` 原本會炸 TypeError；現在 None / list / 空字串 / None-text 全部回傳 None）
- [x] C8. 回傳空內容 / 拒絕分析（safety filter）→ 正確分類為失敗原因 — `tests/hardening/test_c8_safety_filter.py`（5 tests：ValueError on `.text` access / empty text+block_reason / None text+block_reason / batch 續跑 / safety 錯誤不被誤判為 auth fatal）

## D. 批次處理與狀態（pipeline.py / batch_monitor.py / event_store.py / result_store.py）

- [x] D1. 超大批次（500+ 張）記憶體穩定，無 leak — `tests/hardening/test_d1_large_batch.py`（4 tests：600 張端到端 + progress 單調 + FD leak detection + rlimit headroom；整批 <2s）
- [x] D2. 執行中按「取消」→ 當前 inflight 的 request 能中止，資料庫狀態一致 — `tests/hardening/test_d2_cancel_mid_batch.py`（5 tests：pre-cancel / exiftool close / DB 一致 / 多 thread idempotent / events cancelled=True flag）
- [x] D3. 程式崩潰後 resume → 已完成的不重跑，未完成的從中斷點繼續 — `tests/hardening/test_d3_resume_after_crash.py`（4 tests 含 idempotent 回歸 + failed 重試）
- [x] D4. 同一資料夾同時啟動兩次分析 → 鎖定或合併，不會產生衝突狀態 — `tests/hardening/test_d4_parallel_runs.py`（4 tests：兩個 pipeline 併行 / 3×4=12 worker SQLite lock-free / API 409 guard / save_result idempotent）
- [x] D5. 磁碟滿 → SQLite 寫入失敗時優雅處理，使用者看得懂 — `tests/hardening/test_d5_disk_full.py`（5 tests：save_result propagate OperationalError / mark_failed 一致 / 先前 commit 保留 / finally 仍關 ExiftoolBatch / 磁碟釋放後重開 DB 可讀）
- [x] D6. SQLite 檔損壞 → 偵測 + 提示備份 / 重建，不直接 crash — `tests/hardening/test_d6_sqlite_corruption.py`（5 tests，**修掉一個 bug**：`_resolve_db_path` 漏接 OSError 讓 parent 為檔案時 crash）
- [x] D7. 執行中關閉 pywebview 視窗 → 背景任務能安全中止或完成 — `tests/hardening/test_d7_window_close.py`（7 tests：stop_background_monitor 未啟動時 no-op / idempotent / atexit 註冊 / cancel 跨 thread 可見 / cancel idempotent / cancel 能解開 pause / ExiftoolBatch.close idempotent）

## E. 設定與憑證（config.py / secret_store.py / auth.py）

- [x] E1. 首次啟動（無任何 config）→ UI 引導設定，不 crash — `tests/hardening/test_e1_first_launch_no_config.py`（6 tests：pristine home / empty home / dir auto-create / save round-trip / no-key non-blocking / app_version default）
- [x] E2. config.json 損壞 / 格式錯誤 → fallback 到預設值 + 提示 — `tests/hardening/test_e2_corrupt_config.py`（7 tests，**修掉啟動 crash bug**：以前 corrupt config.json 直接讓 app 開不起來。現在 fallback 到 defaults + 隔離到 `config.json.bad` 給使用者救回）
- [x] E3. API key 不存在於 Keychain → 清楚提示去哪裡設定 — `tests/hardening/test_e3_missing_api_key.py`（6 tests：空 key 不 raise + load_config 不 block + cache 行為 + 空 key 透過 C3 auth halt 正確終止）
- [x] E4. API key 設定後立即可用，不需重啟 — `tests/hardening/test_e4_api_key_live_swap.py`（8 tests：set→get 即時 / load_config 反映新 key / clear 即時 / `_get_client` cache 每 key 隔離 / 空 key 不 leak 到真 key / PUT /api/settings 後立即 GET 看到 `set=True` / masked "..." 不洗掉真 key / folder_watcher 每張重讀 config）
- [x] E5. 多個 API key 切換（例如免費 / 付費帳號）正確隔離 — `tests/hardening/test_e5_multi_key_isolation.py`（5 tests：不同 key → 不同 client / 8 thread 併發只構 1 個 / 切換不互相覆蓋 / 空 key 與真 key 隔離 / 20 key rotation identity 穩定）

## F. Metadata 寫入（metadata_writer.py）

- [x] F1. exiftool 未安裝 → 啟動時偵測並提示安裝步驟 — `tests/hardening/test_f1_exiftool_missing.py`（5 tests，新增 `check_exiftool_available()` + `ExiftoolMissingError` 典型例外）
- [x] F2. exiftool 寫入失敗（權限、檔案鎖定）→ 錯誤記錄但不污染 result_store — `tests/hardening/test_f2_exiftool_write_failure.py`（4 tests：write fail → failed 非 completed / 不污染 DB / mixed batch 續跑 / resume 可重試）
- [x] F3. 大量並發寫入同一檔案的 race condition — `tests/hardening/test_f3_metadata_race.py`（3 tests：16 parallel 不同檔 write 全綠 / 同檔 20 並發 rename-wins 檔案可讀 / 交錯 read/write 不 scramble stdin protocol）
- [x] F4. 寫入 non-ASCII 關鍵字（中文、日文、特殊字元）編碼正確 — `tests/hardening/test_f4_non_ascii_metadata.py`（5 tests，**修掉真實 bug**：IPTC Keywords 預設 Latin-1 編碼，CJK 寫入後讀回變 `??`。加 `-IPTC:CodedCharacterSet=UTF8` 標記 UTF-8 payload）
- [x] F5. 超長描述（> IPTC 限制）自動截斷或警告 — `tests/hardening/test_f5_long_description.py`（5 tests：3000 bytes caption OK + XMP 不截 / 20000 bytes 不 crash / CJK 截斷 UTF-8 safe / 1150 bytes headline / 200-byte keyword 不害批次 abort）
- [x] F6. dry-run 模式真的不寫入 — `tests/hardening/test_f6_dry_run.py`（4 tests：SHA-256 檔前後不變 / ExiftoolBatch 零建構 / SQLite 仍存結果 / write_metadata=True flip 確實呼叫 write）

## G. 報告與匯出（report_generator.py / pdf_report.py / api/export.py）

- [x] G1. CSV 含特殊字元（逗號、引號、換行、emoji）正確跳脫 — `tests/hardening/test_g1_csv_special_chars.py`（7 tests，含 G2/G4 相鄰驗證）
- [x] G2. JSON 報告 Unicode 正確（非 ASCII escape） — `tests/hardening/test_g2_g3_g4_g5_reports.py::test_json_*`（2 tests：CJK 不 escape 成 \uXXXX / emoji round-trip）
- [x] G3. PDF 產出中文不變方塊 — `tests/hardening/test_g2_g3_g4_g5_reports.py::test_pdf_*`（2 tests：bundled font 存在 / generate_report 嵌入 NotoSansTC 字型到 PDF stream）
- [x] G4. 空結果 → 產出空報告不 crash — `tests/hardening/test_g2_g3_g4_g5_reports.py::test_empty_*`（3 tests：CSV header-only / JSON `[]` / PDF summary-only 仍合法）
- [x] G5. 匯出路徑不可寫 → 清楚錯誤訊息 — `tests/hardening/test_g2_g3_g4_g5_reports.py::test_*_to_*`（3 tests：readonly dir / nonexistent parent / dir-not-file 皆 raise）

## H. Web UI 與 API 邊界（web_ui.py / api/*.py）

- [x] H1. CSRF / 非預期 origin 請求被拒絕 — `tests/hardening/test_h1_csrf_defense.py`（11 tests：DNS rebind / missing host / wrong port / cross-origin / timing-safe token / POST 同樣 gate / SSE query token 仍需 host check）
- [x] H2. 大 payload（上傳巨量路徑清單）限流或拒絕 — `tests/hardening/test_h2_large_payload.py`（4 tests，**加入 MAX_CONTENT_LENGTH=10MB**：以前沒有任何上限，買菜錢級別 OOM 風險）
- [x] H3. SSE 連線中斷後前端能恢復，不卡住 — `tests/hardening/test_h3_h4_sse.py::test_full_queue*, test_client_reconnect*, test_concurrent*`（slow queue 被 prune / reconnect 只收新事件 / 併發 broadcast+register 無 race）
- [x] H4. 並發多個 SSE client 接收相同進度，資料一致 — `tests/hardening/test_h3_h4_sse.py::test_broadcast_*, test_multiple_events*`（5 queue fan-out / CJK payload 不被 escape / 3 client 接收順序一致）
- [x] H5. API 400/404/500 錯誤回傳結構一致（前端能正確顯示） — `tests/hardening/test_h5_error_response_shape.py`（12 tests，掃 analysis/batch/results/settings endpoints；所有 4xx/5xx 都有 `error` string 欄位；meta-test 確保 assertion 能抓到 HTML 回應）

## I. 打包與發行（build_app.py / HappyVision.spec）

- [ ] I1. 新增 blueprint / module 後 `make app` 不漏 hidden-import（專案有前例）
- [ ] I2. 打包後的 .app 在未安裝 Python 的 Mac 上能跑
- [ ] I3. 打包後的 .app 能正確讀寫使用者資料夾（非 sandbox 路徑）
- [ ] I4. 打包後的 .app 能找到 exiftool（系統 PATH / 內嵌）

## J. 需人工測試（SKIP）

以下項目自動化成本過高或需實際 UI 操作，標記給同事實測時重點觀察：

- [SKIP-需人工] pywebview 視窗在不同螢幕解析度 / DPI 下的 UI 顯示
- [SKIP-需人工] 檔案選擇器 UX（與 Bobo 偏好「圖形化選擇器、不能手打」相符）
- [SKIP-需人工] 長時間執行（> 1 小時）的 UI 穩定性
- [SKIP-需人工] macOS 權限彈窗（完整磁碟取用、檔案存取）流程
- [SKIP-需人工] 拖放資料夾進視窗的互動
- [SKIP-需人工] 暗色模式 / 淺色模式切換顯示

---

## 進度

**總項目：** 55（可自動化）+ 6（需人工）
**已完成：** 53 / 55（+2 追加發現 H5a, A5a）
**最後更新：** 2026-04-19 F5+F6+G2+G3+G4+G5+H3+H4 ✅（報告匯出 + SSE 邊界全覆蓋）

## 追加發現

（ralph 若在過程中發現新 edge case，加在這裡）

- [ ] H5a. Flask 預設 405 Method Not Allowed 回 HTML，破壞 H5 的 JSON shape 合約。前端碰到 method mismatch 時會看到 `undefined` toast。修法：註冊 Flask errorhandler(405) 回 JSON。低風險（UI 不會正常打錯 method），但上線前可以補上。
- [ ] A5a. APFS case-insensitive 但 `is_processed()` 走字面 string match — 同一實體檔透過不同 case path 會被當成新照片重做 Gemini 分析。目前靠「檔案選擇器永遠回 canonical case」繞過。根治需要 macOS `F_GETPATH` fcntl 拿 canonical path，成本偏高。低風險（Finder 拖進來的 case 穩定）。
